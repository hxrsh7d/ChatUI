# Base path/URL the frontend uses for all backend API calls.
# Leave as /api to use Vite's dev proxy (see vite.config.ts) which forwards
# to http://localhost:8000. Set an absolute URL (e.g. https://api.example.com/api)
# when the backend isn't served from the same origin.
VITE_API_BASE=/api
